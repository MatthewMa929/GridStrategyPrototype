//will move differently
//run from player if 1 tile adj
//will try to go to range to hit player
if obj_player.y == y and global.game_state.turn == "enemy" {
	//shoot
	return;
}
if obj_player.x == x and global.game_state.turn == "enemy" {
	//shoot
	return;
}
